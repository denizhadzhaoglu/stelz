# Secrets & credentials
.env
.env.*
!.env.example
.env.bak
client_secret*.json
gcp-oauth.keys.json
.google-calendar-mcp-token.json
token.json
credentials.json

# Claude Code local
CLAUDE.local.md
.claude/settings.local.json
.claude/worktrees/

# Dependencies
node_modules/
.next/
.vercel

# Generated / build artifacts
projects/spot-the-brand/cli/generated/
projects/spot-the-brand/api/dist/

# Temp & cache
.tmp/
.DS_Store
*.pyc
__pycache__/

# Session data
.firecrawl/
.playwright-mcp/
supabase/.temp/

# Large backups (kept locally, not in git)
.backups/

# Eval golden set: third-party openly-licensed images, fetched not vendored.
# golden_sources.json (URLs + licenses + sha256) IS committed — rebuild with
# `python3 tools/eval/fetch_golden.py`.
tools/eval/golden/
tools/eval/sheets/
tools/eval/.cache/
tools/eval/.verify-cache/

# Python venv + web build output
venv/
projects/stelz-brand-watch/web/dist/
firebase/.firebase/

# Local Cloud Functions venv (created for deploys; ~200MB)
firebase/functions/venv/
# Staged hosting build — generated from projects/stelz-brand-watch/web/dist
firebase/web-dist/
